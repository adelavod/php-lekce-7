<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">

        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <meta name="description" content="">
        <meta name="author" content="">
        <link rel="icon" href="favicon.ico">

       {% block title %} <title>Lekce 7</title> {% endblock %}

        <!-- Bootstrap core CSS -->
        <link href="css/bootstrap.min.css" rel="stylesheet">

        <!-- Custom styles for this template -->
        <link href="starter-template.css" rel="stylesheet">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
    {% block navigation %}Tohle je horní blok. Vychází z base templatu. Je všude.{% endblock %}

    {% block body %}

        <style>
            .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
            .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
        </style>

        <div class="example-wrapper">
            <h1>Hello {{ controller_name }} : seznam jazyků</h1>

            <a href="{{ path('trest_known') }}">známé jazyky</a>

            <table>
                <thead>
                <th>Jazyk</th>
                <th>Umím</th>
                </thead>
                <tbody>
                {% for language in languages %}
                    <tr>
                        <td>{{ language.testString }}</td>
                        <td>{% if language.testBoolean %}ano{% else %}ne{% endif %}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>

    {% endblock %}

    {% block footer %}THIS IS FOOTER. Vyrobeno v dubnu.{% endblock %}

        {% block javascripts %}{% endblock %}
    </body>
</html>
